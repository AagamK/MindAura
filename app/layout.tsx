import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { AuthProvider } from "@/lib/auth"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "MindAura - AI-Powered Mental Wellness",
  description:
    "Your personal mental health companion. Chat with our AI assistant, track your progress, and get personalized insights for better wellbeing.",
  keywords: ["mental health", "AI", "wellness", "therapy", "support", "mindfulness"],
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  )
}
